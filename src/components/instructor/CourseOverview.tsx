import { Box } from "@mui/material";

export const CourseOverview = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: 200,
        backgroundColor: "#fff",
      }}
    >
      OVERVIEW
    </Box>
  );
};
